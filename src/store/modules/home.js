import {RECEIVE_HOMEDATA} from '../mutation-types'
import {homeData} from '../../api'

const state={
  homeData:{}
}
const mutations ={
  [RECEIVE_HOMEDATA](){

  }
}

const actions ={

}
const getters={}

export default {
  state,
  mutations,
  actions,
  getters
}

